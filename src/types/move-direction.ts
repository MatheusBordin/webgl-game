/**
 * Move direction itnerface.
 *
 * @export
 * @interface IMoveDirection
 */
export interface IMoveDirection {
    left: boolean;
    right: boolean;
    front: boolean;
    back: boolean;
    up: boolean;
    down: boolean;
}